# Core dependencies for Heisenberg Team skills
# Install: pip install -r requirements.txt

# PDF generation (minimax-pdf skill)
reportlab>=4.0
fpdf2>=2.7
pillow>=10.0

# Data & documents (minimax-xlsx, minimax-docx skills)
pandas>=2.0
openpyxl>=3.1

# Telegram analytics (channel-analyzer skill)
telethon>=1.30
